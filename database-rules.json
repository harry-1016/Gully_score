{
  "rules": {
    "matches": {
      ".read": true,
      ".write": true,
      "$matchId": {
        ".validate": "newData.hasChildren(['teamA', 'teamB', 'overs', 'matchStatus'])"
      }
    }
  }
}
